import AuthPage from '../../components/Auth/AuthPage';

export const metadata = {
  title: 'BytBoom — Login',
  description: 'Access your BytBoom trading dashboard',
};

export default function LoginPage() {
  return <AuthPage />;
}
